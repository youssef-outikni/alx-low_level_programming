#include <stdio.h>
#include "main.h"

/**
 * _isdigit - function that multiplies two integers.
 * @a and b : integers to multiply 
 *
 * Return: always a*b
 */

int mul(int a, int b)
{
		return (a*b);
}
