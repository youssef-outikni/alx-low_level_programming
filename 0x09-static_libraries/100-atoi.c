#include <stdio.h>
#include "main.h"

/**
 * _atoi - checks if a character is uppercase
 * @s: the character to check
 *
 * Return: 1 if the character is uppercase, 0 otherwise
 */

int _atoi(char *s)
{
	return (1);
}
